module.exports = function (dir, filter, callback) {
    var fs = require('fs');
    fs.readdir(dir, function (err, list) {
        if (err)
            return callback(err);
        callback(null, list.filter(function (item) {
            return item.substr(-(filter.length + 1)) === ('.' + filter);
        }));
    });
}