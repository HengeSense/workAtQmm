var $get = process.argv,
    dir = $get[2],
    filter = $get[3],
    list = require('./listFiles_module.js');

list(dir, filter, function (err, data) {
    if (err) 
        return console.log(err);
    data.forEach(function (item) {
        console.log(item);
    });
});

